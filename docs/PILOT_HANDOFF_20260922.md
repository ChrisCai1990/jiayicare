# 单客户灰度交付与操作清单

## 已发布：03100586（2026-09-22）

- 功能分支与GitHub master已同步，标准deploy.py --skip-data-migrations部署成功，线上HEAD精确一致；四端构建完成，后端online且重启后复查稳定。
- API健康、医护端、管理端、嘉医用户端HTTP200；未登录岗位接口401。进程继承环境与dotenv合并核对为allowlist/仅1人/金娟true/未列入false；启动日志证实禁止自动schema写入。新恢复、自动双岗准备、阶段扫描仍关闭，旧后台任务未关闭。
- 部署脚本列出的jinyisen.jiaycare.com无法DNS解析，指定本机连接时证书不匹配；该域名不计公网验收通过，本次不改DNS/证书。
- 没有登录生产岗位代操作，没有创建测试订单/报告、发送通知、代做临床审核或核销。隔离环境原同案例证据不等于金娟真实生产全程完成。

### 用户验收从这里开始

刷新医护端，用金娟所属健康顾问账号打开其年度管理方案，先核对准备清单及年度综合健康评估；资料不足由实际负责人员补充。完成专业评估后审核方案，再按下文六步流转。双岗自动准备开关仍关闭，不能宣称已自动按日期派发；首次验证准备情况后再显式决定启用。已发生的就医、报告、结果判断及服务核销须按真实业务办理。

最小回退先关闭白名单入口和新增扫描、保留记录，版本基线c29e8942及服务器私有备份已留存；不覆盖新业务数据。

## 已验证

- 最新生产解析修复c29e8942已纳入；1197项后端通过、0失败、2项独立Mongo用例跳过；员工端1904模块构建通过。
- 双客户隔离：新年度准备/持续随访只对选中客户生效；未选中客户旧年度及完成入口保留。
- X9ca9X同一标准体检案例：客户问卷、两岗准备、承接、岗位执行、报告审核、顾问一次确认、最终验收、合成付费状态下核销、原随访自动结束通过。
- 规划师七环节详情及健管原任务结果依据已实际浏览器检查；门诊审核接续与代诊派单回归通过。
- 真实AI入口的既有证据在LIVE_AI_ENTRANCE_ACCEPTANCE.md，为另一案例。本次没有再次调用真实AI；不能把合成报告和履约输入当真实临床全链。

## 本地查看

页面：http://localhost:5174 。当前保留虚构客户的健管原体检随访结案详情。测试账号仅存于本机临时会话清单，不提交或在文档公开口令。

会话清单：`C:/Users/huawei/AppData/Local/Temp/jiayicare-acceptance-X9ca9X/session.json`。
后端仅127.0.0.1:3000，Mongo仅127.0.0.1:27134，数据库为该清单指定的独立随机库；网络出口封锁，无生产凭证。重启用startIsolatedAcceptance.js的ISOLATED_ACCEPTANCE_SESSION模式，已校验并恢复同一白名单，不能默认改为all。

## 发布前置执行记录（2026-09-22）

用户明确确认完整候选目的地及金娟白名单发布，原上传审批阻塞解除；29fd9326已同步功能分支。生产与远端master复核为c29e8942。

私有备份`/var/backups/jiayicare/pilot-20260922-29fd9326`包含代码bundle、四端页面、配置与数据库。dump期间fsync锁已释放；gzip完整性及mongorestore dryRun通过，未做完整恢复演练。

16项指定索引已新增，21项均存在且无冲突。单客户白名单及新扫描关闭配置已写入，实际生效须新进程验证。未写客户业务数据；代码发布仍以随后部署日志为准。

### 已解除的审批记录

安全审批拒绝向GitHub上传完整候选代码，要求用户明确批准目的地和内容：
`https://github.com/ChrisCai1990/jiayicare` / `feature/health-management-foundation-20260918`，内容为已提交候选业务代码及验收文档，不含本机未跟踪脚本、运行凭证或数据库文件。未重试、未以直接生产部署规避。

## 审批后发布步骤（尚未执行，不代表已经上线）

1. 再核对远端master及生产版本；如前进，先整合，保留所有热修。
2. 备份现行代码版本、配置及数据库，验证备份可用；不得承诺“恢复旧数据库一键无损回退”。
3. 重新只读查冲突，按RELEASE_CHECKLIST_20260922.md精确新增16项唯一索引，不syncIndexes/drop/清洗客户数据。
4. 仅将已核验金娟档案ID设为allowlist，生产缺省关闭新闭环；关闭自动schema写入及新恢复/新评估扫描，保留原OCR/提醒。逐项核对新自动准备是否需开，不随部署扩大客群。
5. 审核并发布同一干净master，标准deploy.py --push --skip-data-migrations；健康、版本、岗位及非白名单旧入口冒烟通过后交用户验收。
6. 金娟按真实资料执行：顾问确认年度准备/专业评估→审核方案→两岗体检准备→规划师承接→就医专员履约→健管报告审核→顾问结果处置→最终验收/合法核销→原随访自动关闭。真实通知、真实付款/核销、医疗判断不能用脚本虚构完成。

## 回退边界

首先暂停新闭环恢复及白名单新增入口，保留已产生记录。代码回退以最新生产c29e8942（或发布前新基线）为准；新版任务需核对交接，不能删除或用旧数据库覆盖。营养、运动、中医新增服务验收仍在本轮范围外。
