# 发布前11项旧测试修正

本次只修改测试和说明，不修改生产业务代码、不部署、不调用真实AI。

验证结果：相关28项全部通过；最终全套1184项，1182通过、0失败、2跳过（AI_BUDGET_TEST_MONGO、REPORT_QUALITY_TEST_MONGO未启用的独立Mongo集成用例）。命令为node --test --test-concurrency=4 backend/test/*.test.js，退出码0。此次并非这两项集成用例验收，也不是重新运行真实AI/生产闭环。

| 原失败 | 修正与保留的检查 |
|---|---|
| 研判接口数量1项 | 不再写死6次调用；逐条检查所有现有研判路由均校验客户访问，且不误用阶段评估门槛 |
| 门诊最终验收1项 | 按现行SOP检查顾问结果审核后关闭，不恢复已移除的额外规划师验收；仍保留体检规划师最终验收及备份要求 |
| 岗位/保险任务筛选2项 | 执行真实路由筛选表达式；检查执行、督办、保险历史任务显示，以及非本人、已结束服务、无效订单不显示 |
| 附件预览1项 | 匹配优先预览地址、原文件地址兜底，保留图片/PDF弹窗及下载校验 |
| 客户任务1项 | 执行真实Mongo查询条件的内存匹配；内部任务排除，特定客户就医提醒保留，其他客户和阻塞任务不可见 |
| 住院权限5项 | 补全路由片段运行所需真实辅助模块及模型替身；使用确定性AI替身的两步响应，不将缺依赖500视为权限拦截；顾问本人可保存/派发，其他顾问拒绝且不保存、不派单、不改任务/记录 |

新增taskVisibility辅助器从业务源码提取实际过滤逻辑，缺少边界时直接失败，不维护第二套业务过滤规则。住院推送以派发spy检查调用边界，不能称为真实数据库任务生成或真实AI验收；需要真实HTTP的证据仍以已有隔离验收文档为准。

首次全套运行暴露旧隔离依赖缺少uuid，未修改业务代码规避；复跑使用发布候选按锁文件安装的backend/node_modules和node_modules作为NODE_PATH。不会修改运行中后端的进程环境。
