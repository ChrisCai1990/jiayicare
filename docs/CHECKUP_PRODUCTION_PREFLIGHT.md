# 体检闭环生产只读核查（2026-09-20）

## 环境结果

- 生产Git版本：`8fbda1cd29766e4cb7cffd16300b94d0e28dd181`，已跟踪文件无改动；后端PM2为online。
- MongoDB7.0.34，非副本集、非分片。与隔离测试版本及standalone模式一致，操作系统仍不同。
- PM2环境与后端配置中的 `CHECKUP_PREPARATION_AUTO_ENABLED` 均未启用。
- `followups` 缺 `annualDispatchKey` 稀疏唯一索引；`checkuppreparationhandoffs`、`checkuppreparationsuggestions` 集合不存在。新功能尚未部署，不代表生产数据库异常，也不能绕过索引门槛直接开启派发。

## 指定客户适用性（不在仓库保存客户身份或医疗内容）

- 同名存在归档历史，排除归档后唯一有效档案；三个管理岗位已有分配。
- 当前年度方案为历史已确认记录，体检日期不在14天准备窗口，未携带新自动派发标记。
- 存在原体检方案及普通提醒，但无新准备承接关系；未发现该路径关联订单。
- 因此不能用已有记录直接宣称新闭环已验收，不应为了测试改动真实体检日期、补造报告/订单、核销或重新确认历史方案。

## 安全边界和下一步

本次只执行MongoDB原生读取、Git版本/状态、PM2状态读取；没有加载业务模型、运行迁移/索引创建、部署、派单、推送、付款或核销。个人标识及岗位人员姓名仅在本次授权会话结果中，不写入本文件。

1. 先准备待部署差异和数据库结构变更清单，审阅后单独授权。
2. 部署仍保持自动派发关闭，不能借部署悄悄回填历史客户。
3. 多岗位完整流程优先用独立测试档案/测试环境；若使用真实客户，必须以真实业务安排为依据，具体新增、推送和状态变更需事前明确确认。
4. 上线前还需处理已知测试失败和未覆盖的多候选/多子项目、缺失来源等边界。只读核查不是生产业务验收。

复核脚本：`python scripts/audit_checkup_readonly.py --patient-name <明确授权的客户姓名>`。脚本不修改数据，遇到有效同名多记录时停止深入查询，要求先确认目标。
