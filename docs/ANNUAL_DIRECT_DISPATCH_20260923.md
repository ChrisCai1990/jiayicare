# 年度单项就医协助直接派单

## 本次范围

健康规划师页面改为办理事项、预约交接、安排就医专员。去掉对应随访/实际服务手动选择、重复正文与通用编辑删除。顾问原计划折叠只读；医院科室专家与逐项预约状态自动继承，不重新输入。

只有已确认预约的单项就医协助可派单；全托管及已关联旧服务保留原流程，不重复建单。纯提醒仍仅健管随访。派单创建内部执行任务，不创建收费订单，不自动向客户推送。

## 留痕及权限

规划师所属任务校验、同机构有效就医专员校验；持久化派单意图后以确定性ID创建执行任务，可恢复中断且防重复。原计划、预约快照、派单人员和备注、现场预约结果、执行结果、验收人分别保留。计划同步不能覆盖已派单事项。

就医专员从工作台任务弹窗处理，不扩大客户完整档案访问权。现场预约未登记不能提交结果；执行后交规划师验收，不替代顾问医疗结果审核、不直接结束原随访。客户端不能读取或提交内部年度服务任务。

## 验证与发布边界

新增运行时测试覆盖重复/中断恢复、权限、并发、未预约/旧服务/全托管拦截、现场结果及验收；真实JSX渲染断言页面去重。全量1276项：1274通过、0失败、2跳过；Staff生产构建1921模块通过。发布结果以随后记录为准。

仅金娟白名单试点、跳过迁移。不改历史预约、订单或客户方案，不代用户派单。真实业务验收尚待用户操作。

## 发布核验

2026-09-23已部署a5b1027606a32b70622bc9e5dbb3753b87386347，GitHub双分支/生产版本一致，四端构建成功。公网API/Staff/Admin/用户端200，Staff资源index-DwLvX4-t.js为200且含直接派单入口。金娟allowlist和两项关闭开关保持；跳过数据迁移。未执行真实派单。
