# 真实AI入口单项验收（2026-09-22）

## 范围与授权

用户明确允许临时共用生产服务器通义千问密钥，向阿里云发送纯虚构资料。服务器仅只读获取该单项配置，经内存管道传入独立本地进程；不保存密钥，不加载整份生产环境，不部署、不写生产数据库。

新隔离会话：本机临时目录 jiayicare-acceptance-6CqCL7；实际API端口3002，独立Mongo库，原3000/5174不动。最多6次串行AI请求、150000估算tokens本地预算；仅允许指定千问chat/completions端点，其他HTTP/fetch/生产数据库/子进程出口拦截。结束后3002已停止，未保留带密钥服务。下次真实调用仍需明确授权。

## 实际验收结果

- 报告：虚构来源报告经健管实际审核API，后台实际千问生成“2026-10-22复查血常规并上传结果”草稿；审核前不派正式任务，顾问实际审核发布，重复审核任务数量不增。没有人工替写AI结果。
- 年度：准备不齐409；实际建立、终审专业评估并确认准备清单，千问生成血常规建议且日期与来源相符；真实保存状态pending，经顾问发布approved，重新读取同一方案。健管生成及发布均403。
- 原专业评估、基础档案摘要、标准模板和报告是明确虚构测试输入；不是医学判断质量认证，也未把AI摘要本身当作已验的生成入口。报告入口从已结构化报告审核开始，不涵盖原图OCR。此次为真实HTTP/API，不冒充多岗位浏览器全程操作。
- 最终校验“没有无来源空腹/停药/禁食要求、没有无依据年度全套项目、每条检查建议包含来源说明”通过。输出仍需顾问审阅，提示词改进不能保证所有模型输出永不出错。

## 发现并修复

1. 实际报告入队失败：共享证据锁插件把仅FollowUp具有的outcomeClosureIntent条件加到报告草稿upsert，Mongoose严格校验拒绝。现在只对声明该字段的模型加此条件，所有模型保留证据锁。不是删锁或绕过来源校验。
2. 首次年度生成添加了输入没有的检查准备要求/基础全套项目，未发布。加强来源限定后，第二次仍缺basisSummary；补齐JSON示例与文字约束，第三次年度生成内容断言通过，才保存及审核。第二次年度响应未完整留存，仅留失败断言和实际用量，不虚构该响应证据。

## 证据与用量

临时会话中的live-ai-evidence.json保存首版及最终年度内容、报告草稿、来源ID、正式测试方案ID和调用用量；session.json含合成账号密码，不提交仓库。

实际qwen-plus：报告406 tokens；年度2502、2496、2459 tokens；合计4次7863 tokens。未配置准确单价，不估报人民币金额；供应商账单为准，本地用量不计入生产应用内部统计。

回归：21项年度/报告/出口单测通过；14项合并结果/出口单测通过（其中出口测试重复，不相加称35项唯一测试）；真实Mongo草稿upsert及锁、并发唯一结案、子进程exit44及原输入恢复通过。仅后端/验收脚本修改，未重复前端构建。

## 接续

本条“真实AI入口”完成。最终整链回归、各岗位页面与用户交付仍按原计划，营养/运动/中医不扩入。原本地3000仍旧代码，最终整链测试须用无AI凭证的原隔离启动器重载本次修复；不可为方便把生产配置复制进去。
