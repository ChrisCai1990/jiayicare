# 年度体检“缺少明确内容”修复

## 核查

只读检查金娟最新规则3失败快照，确认annual_checkup.focus实际是非空字符串数组，并非用户资料未准备好。现有校验只接受字符串，旧focus单字段补正无法一并处理来源关联及“已安排、不重复”说明，最终仍失败。先前仅从错误提示推测内容为空，不是此次确切原因。

## 修复

- 纯字符串列表无损转为逐行文字；混合对象列表及对象不强制字符串化，保留内容交校正/校验。
- 同次请求最多一次45秒年度模块校正，替代旧focus单字段补正；只允许修改annual_checkup及evidenceCoverage，其他模块保留原样。
- 区分实际年度项目和不再重复安排的内部说明；校正来源ID与覆盖状态。没有依据的年度模块可返回空对象，但included来源仍必须有其他对应事项，不允许隐性遗漏。
- 校正结果再次通过原模板、来源和日期/去重校验；失败输出留在失败快照，不更新客户方案。对象/畸形JSON/错误覆盖均不能通过。
- 规则版本4使旧规则缓存不复用；同输入成功结果继续持久复用。90秒首调用加45秒单次校正，保持已有180秒入口超时，不增加第三次调用。

## 验证边界

11项专项通过，包括同形状列表回归、重复说明校正、无依据空年度模块、错误来源覆盖拒绝、其他模块不可被校正改写。
未代客户调用真实AI、保存、确认或推送；不修改报告或历史客户方案。全量及部署结果追加如下。

全量1239项：1237通过、0失败、2项独立Mongo集成跳过；后端语法与diff检查通过。本次仅后端代码变化，按标准脚本跳过数据迁移发布。
