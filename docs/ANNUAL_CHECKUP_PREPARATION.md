# 年度体检提前准备：分步实现

## 当前边界（2026-09-19）

已完成准备窗口及双岗位候选的确定性计算，以及旧每日扫描误改派的修复。**尚未接入年度确认、每日派发或工作台处理入口，不表示已经自动生成两岗位准备任务。** 未部署、不迁移历史任务，不创建体检订单，不调用 AI。

`annualCheckupPreparation.js` 是纯计算模块，输入年度方案、客户、`annualPeriodicGate` 的完整结果和当前时间。返回 `state`、任务候选、阻塞原因；本身不读写数据库。`tasks` 不是已派发记录，`issues` 也还没有投递至岗位工作台。

## 已验证的准备规则

- 按北京时间，以体检日前14天进入窗口；此前不铺设任务。
- 准备起点不得早于客户确认、有效执行起点、当前可信服务期开始。确认较晚或续年起点较晚，立即准备并标记不足14天。
- 两个并行候选：所属健康顾问准备并审核体检方案；所属健康规划师沟通时间和体检资源。无互相前置依赖、不转给健管专员、不生成客户打卡任务。
- 两岗位都适用于体检模块，不依赖是否购买就医协助；实际服务仍沿原授权和订单流程。
- 缺少岗位时返回明确问题，不用创建人或健管专员冒充；另一有归属的岗位仍有候选。
- 日期必须明确、有效且在服务期内。已过体检日期不补发新预约任务，返回顾问核对事项；当天可准备。
- 改期采用已有有效日期覆盖层，任务键仍取冻结原日期，纳入既有改期影响检查。不修改冻结方案。
- 候选身份和排期不随每日扫描漂移。但**数据库唯一派发与失败恢复尚未接入**，不能以候选稳定等同真实数据库幂等验收。

## 已接入的旧逻辑修复

原每日扫描仅凭标题包含“就医提醒/年度体检”就把任务改派给健管专员，会影响同名顾问、规划师和服务流程任务。

现在只校正带年度来源的 `scheduled` 普通提醒，且尚未开始、无岗位流程标识、无服务/订单来源、无服务关联且未阻塞。写回还检查原负责人、状态及上述来源条件，避免读取后已经完成、人工改派或关联服务的记录被迟到更新覆盖。没有批量修复以前可能已误改的历史数据，历史归属需另行只读核查。

## 下一步接入要求（尚未实现）

1. 工作台直接进入该岗位的实际业务：顾问创建/复用体检方案、审核保存；规划师保存沟通的时间和机构资源。以实际业务证据自动完成准备，不新增空的“点击完成”步骤。
2. 两岗位结果汇合后，复用既有体检预约/执行链及健管随访回写；不能在准备期自动下单。与已有服务需求、已建体检服务去重，不能并行出现两套同义待办。
3. 接入前明确新旧方案适用边界，旧已确认方案不直接全量回填。候选用 `annual_service` 来源，必须同步调整 `annualPlanServiceTasks` 的取消清理边界，否则它会取消不在通用服务需求列表中的准备任务。
4. 在现有每日扫描和确认同步中接入，不加新高频定时器。使用已核验唯一索引及稳定派发键、重试和续年同步屏障；缺岗/日期异常要进入明确岗位工作台。
5. 准备任务派发后不能直接套用仅移动体检日的旧改期逻辑：其准备日与体检日不同。必须扩展成一致改期或保守阻断；不能只改部分任务日期。
6. 用真实数据库及多岗位页面验收普通、临近、已准备、已有服务、续年、缺岗、取消/退回、改期、并发和进程中断场景，再讨论启用。

## 验证

新增22项测试：`annualCheckupPreparation.test.js` 18项、`annualReminderAssignment.test.js` 4项。与年度派发/续约/改期、动态随访、阶段评估、原体检、支付/订单/补给、消息等相关回归合计419项通过。使用本地模拟数据；没有真实数据库写入、浏览器验收或真实 AI 调用。本轮无前端改动。
