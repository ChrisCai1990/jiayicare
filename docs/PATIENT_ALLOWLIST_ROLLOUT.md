# 单客户白名单：隔离收尾证据与待发布审批

## 当前结论（以本节覆盖下方历史进度）

本地候选已合入生产/GitHub最新 `c29e8942`，整合提交 `a74c45fd`。合并后后端1197通过、0失败、2跳过。没有生产发布或索引写入。

第二个独立双客户会话 `X9ca9X` 已重载合并候选，白名单/非白名单真实API再次通过。使用标准体检模板及实际客户问卷API，同一案例两岗准备→客户确认→服务承接→预约/现场→报告回收与审核→顾问一次确认→最终验收→实际本地核销API→原健管随访自动结案通过。并发核销仅一次成功，订单剩余1/2次；重复409，不改原结案时间。七个服务任务全部完成、四岗服务工作台无该服务残留。支付状态和报告/履约结论是明确合成输入，未使用真实付款或真实临床决定。

同会话门诊服务审核生成唯一健管后续并完成原计划、防重复通过；代诊原预约/规划师派单实际HTTP回归通过。最初门诊脚本新建了不在白名单的虚构客户，正确走旧分支；已改为复用被选中的虚构客户后验证新版路径。核销脚本最初漏传`--merged-outcome`导致旧分支预期失败，保留原数据，按正确已实现协议重跑通过；未为测试改业务状态。

实际页面：顾问核对双客户年度入口；规划师首页无该服务残留、详情显示七环节已完成；健管原体检随访显示已完成及顾问结果处置依据。测试客户尚有用药/营养素准备、独立边界任务、年度统筹和新门诊后续，均不是已结服务残留，不伪造全部客户任务清零。

GitHub功能分支上传被安全审批明确拒绝：需要用户批准将完整候选代码及验收文档上传到 `https://github.com/ChrisCai1990/jiayicare` 的 `feature/health-management-foundation-20260918`。不换通道重试、不直接部署规避远程历史要求。审批后才继续备份恢复检查、16项精确防重索引、单客户配置、标准发布与生产验收。操作交付见 `docs/PILOT_HANDOFF_20260922.md`。

## 2026-09-22 本次收尾证据

- 已补持续随访能力的服务端计算字段及页面分流、门诊服务旧审核分支、服务期旧到期规则、体检/报告恢复队列、续约补给与工作台读时范围；非白名单手动阶段评估保留月度/营养师初审，不启用新年度总评与多领域入口。
- 新虚构双客户会话 `CTanZJ`：五岗位真实登录/工作台通过；白名单复查提前结束409、追加过程200；非白名单沿用完成200、过程403，年度准备为空且无新准备任务。浏览器顾问实际核对两个年度页面，新面板只出现在白名单。
- 同一白名单虚构客户实际HTTP：客户确认、两岗准备、方案发布、服务承接、预约/现场节点、报告审核、顾问一次确认无需后续、规划师最终验收、原健管随访自动结束通过；重复确认不重建/改完成时间，四岗无该服务残留。此为自定义无订单服务输入；未冒充真实AI/真实履约/收费核销或标准套餐全覆盖。
- 旧 `checkupIntakeGateHttp` 是“缺问卷”的专用失败场景，误用于上述自定义场景前置校验失败，未执行其业务请求；不计通过。旧闭环脚本遗漏日期/机构，被现行审核保护正确拦截，已改为通过真实编辑接口补充明确虚构元数据，再验收通过；没有放宽审核规则。
- 后端1199项：1197通过、0失败、2个独立Mongo配置用例跳过。员工端1904模块构建通过。证据保存在本机会话的 `pilot-boundary-http.json`、`preparation-http.json`、`service-http.json`、`closure-http.json`。
- 发布前只读复核发现生产/GitHub master已更新至 `c29e8942`（报告解析熔断隔离修复），不得用43781626旧候选覆盖。21项唯一索引仍无冲突，16项缺失尚未创建。生产未写入/部署，后续还须合入新基线、再回归及发布保障检查。

2026-09-22 用户确认仅金娟进入新版完整闭环，其余客户保持原流程。生产只读核验唯一有效档案及三岗位已分配；真实ID仅用于部署配置，不写入测试样例。无生产写入、部署或推送。

## 局部已实现

- HEALTH_MANAGEMENT_ROLLOUT_MODE=allowlist + HEALTH_MANAGEMENT_PATIENT_IDS（逗号分隔精确ObjectId）。生产无配置、模式错误、名单含无效项均关闭。不按姓名/请求体标记启用，原权限仍须校验。
- 报告保存的新随访事件、专业反馈评估任务、报告/评估生成及恢复队列、动态随访发布增加客户限制。
- 报告草稿全部修改（含拒绝/接管）、专业评估创建/审核、年度准备、续约凭据接口限制；非白名单报告/评估列表为空，准备读取不派任务。
- 两岗体检准备按数据库方案所属客户判断，不信任调用者传入patientId；审核任务自动完成也加客户过滤。
- 服务关联入口核验持久任务归属；后台/工作台关联恢复将客户范围与调用方筛选条件取交集，不能覆盖筛选。非白名单不投影服务状态、不追加随访过程、不审核结果、不释放已完成任务的证据锁。
- 白名单测试为单元/内存路由证据，不等同真实API、AI或生产全链验收。

验证：新增6项白名单测试通过（含持久所属客户与请求体伪造、拒绝入口、非白名单零AI/派单）；最终后端1190项，1188通过、0失败、2项独立Mongo配置用例跳过。staff/report路由语法与git diff检查通过。尚未重载隔离实例或做新白名单页面验收。

继续项：随访过程/结果相关35项定向回归通过；另补服务恢复筛选交集测试。年度兼容核查已定位staff.js的保存冻结/年度总评来源、生成准备门槛和专业评估提示、发布唯一正式方案门槛仍全局生效，须成组分流，不能只跳过准备检查便声称旧行为一致。当前尚未修改这些年度分支。

上述接续后全套1192项：1190通过、0失败、2项独立Mongo用例跳过；仍仅单测/内存行为，不是白名单真实全链验收。

## 发布前仍须完成

接续更新：年度保存的总评来源/冻结检查、生成准备与专业评估输入、发布的准备/唯一正式方案门槛已按客户分流；非白名单客户确认沿用旧路径，不新增冻结/自动准备意图。年度页面按后端返回的enabled控制新面板及按钮，切客户的旧请求响应不回写。3项路由/确认测试及4项既有确认测试通过；另1项实际按钮片段React渲染测试通过。全套1195项1193通过/0失败/2跳过（不含最后新增的独立渲染测试）；员工端1904模块构建通过。不是实际HTTP/整页验收，仍不能发布。

1. 旧流程兼容：原年度派单、持续随访完成、服务审核/承接、续约访问、工作台读时恢复仍需逐一核对。非白名单须保留旧行为，不能用403代替原业务；年度主入口本轮已分流但仍待实际验收。
2. 前后端一致：年度页入口已控制，其他工作台/患者页的新入口仍需核对；选中/未选中虚构客户的实际API、队列、多岗页面与旧链路回归。
3. 部署：复核最新master/生产、可恢复备份、16项唯一索引冲突及创建、明确配置后标准最小发布，再执行金娟全程验收。真实通知、支付、核销不作为自动测试动作，不伪造已完成状态。

恢复开关HEALTH_MANAGEMENT_RECOVERY_ENABLED=false不是完整白名单。当前局部修改尚不能保证关闭配置即可无损回退，有新版记录须保留核对。禁止直接部署整个功能分支并称为单客户隔离已完成。
