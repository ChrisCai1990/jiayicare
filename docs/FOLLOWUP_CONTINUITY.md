# 持续随访变更（2026-09-21）

> 2026-09-22门诊服务审核至原计划结案已接：沿同客户唯一FollowUpServiceLink（health_plan）复用顾问原提交，确认资料齐全、冻结已审报告摘要、唯一健管后续落地后以既有证据锁完成原随访；Link也纳入锁，失败保留原计划，未另建AI草稿或审核任务。37项单测、serviceOutcomeHttp实际API完整提交/重复/未确认拦截及既有mergedOutcomeHttp/followUpOutcomeHttp回归通过，1902模块构建通过。输入为合成服务/已审报告，不是真实AI/预约/完整多岗页面验收。仅门诊一站式此路径，不冒充体检及其他服务全覆盖。主session/service-outcome-http.json留证，后端exec53635。硬中断时涉及服务审核锁的人工恢复尚未做真实故障回归，不承诺自动接续。未部署/推送，下一步同案例页面终验与原体检链核对，不扩功能。

> 2026-09-22服务结果衔接第一步：发现门诊一站式顾问完成后直接create重复后续且派给顾问；新增服务审核来源确定性主键、仅插入、派所属健管、持续随访标记及来源凭据。完成前验证顾问归属/健管/日期，历史已完成无新版凭据拒绝自动重建，完成重试不得换内容。26项单测及隔离Mongo10并发唯一任务、重试完整保留已完成记录通过。尚未重载真实HTTP/页面验证，本步不等于服务审核合并原计划结案完成；后端73332仍上一版。未部署/推送，不改历史生产数据。

> 2026-09-21结案依据并发保护：FollowUp/MedicalReport/ReportFollowUpDraft增加内部证据占用，结案前锁定并比对完整快照（含同毫秒变化），父任务token屏障、冻结原输入；仅原提交显式重试，未接自动恢复。隔离Mongo更新/save/文档与查询删除/父任务修改拒绝、并发唯一成功、真实子进程exit44后原输入恢复通过；78项针对性单测、重载86975后合并审核/客户求助/来源候选实际HTTP通过。扩大全套单测存在失败，尚未完成基线归因，不称全绿。服务自有结果审核合并、真实页面终验仍待，整体未闭环。无生产变更/推送，自动任务暂停。

用户确认：同一管理事项多次沟通不结束；完成检查、报告审核、顾问处置后才结束；需要后续计划时须实际发布成功，无需继续时明确审核结论。服务履约/核销与管理闭环分开，不增加重复操作。

## 第二部分最新进展（未部署）

合并审核（报告草稿路径）：FollowUpOutcomeReview允许选择ready待审核或已审核草稿，核对/编辑后一次点击，submitMergedOutcome先调用既有review发布，再提交原计划outcome-review。发布失败、内容与服务端不一致、原任务版本冲突都不关闭原计划；重试不另建后续。无需继续可审核空草稿并保留sourceDraftId，后端验证已发布空结论/来源当前有效，不能拿有后续的草稿直接声明无需继续。

17项单测及mergedOutcomeHttp实际接口通过：新计划含规划师服务承接任务、无后续两路径与重放，使用UI同一编排函数，初始报告/草稿为合成，非真实AI。证据主session/merged-outcome-http.json；后端21412。1901模块构建通过（末次纯逻辑内容一致保护之后单测/HTTP通过）。真实页面点击仍待。这是前端两个真实请求的顺序编排，不是跨文档事务/后台恢复；不得宣称关闭并发窗口。既有服务自有结果审核节点未合并，仍禁止为有关联服务的报告另建重复AI草稿。

来源预选已实际验证：后端42063，followUpCandidatesHttp同主session既有合成服务验证1份明确关联已审报告、健管403、无来源空、读接口不写原任务。computer-use顾问详情仅对应报告预选，其他报告不勾选，后续安排/完整资料声明不默认，结案按钮禁用，未提交结案。证据主session/followup-candidates-http.json；任务6ab0f76e2cdf8184a7f152a8。仅HTTP/页面来源预选通过，合并审核及跨文档保护仍待。

报告预选新增：GET /staff/followups/:id/outcome-candidates仅所属顾问/superadmin可读；沿FollowUpServiceLink或精确年度冻结排期的唯一准备任务/active承接读取服务ID，再查同客户已审核报告。仅当前版本且已发布审核草稿可作后续候选，唯一时预选；无明确来源不猜测。只减少选择，不自动确认资料齐全/无需继续，也不关闭原计划。29项针对性测试和1900模块构建通过；待重载后实际API/页面验证（24274未加载本次改动）。

兼容实测完成：后端24274已加载补丁；followUpHelpHttp实际客户认证/健管登录通过，求助保留状态和服务锁，禁止客户完成/已关闭重开，等待期间保存过程。computer-use健管患者页实际追加第二条沟通，重开显示两次过程，仍“随访中”，无完成选项。证据主隔离session/followup-help-http.json，客户6aaf5f24ffc92a5f30a5d2ab、任务6ab0f0130e930fd7ee7f26ae。认证令牌仅隔离启动器生成、2小时有效，不输出服务端密钥，不新增生产认证入口。此验证不等于报告自动关联/真实AI/全链完成。

最新兼容补丁：持续计划的客户needFollowUp=true走独立CAS，只标记人工跟进并提前remindAt，不改计划状态/结果/服务锁；并发结案冲突返回409。患者页结构化原管理计划即使服务等待锁定，也可打开过程填写，岗位服务任务仍锁定。24项针对性测试及1900模块构建通过，接口/页面新增入口待重载复验；不迁移历史数据。

- 结构化年度检查/复查/就医及报告派生医疗计划已阻断提前完成，服务完成/核销不关闭原管理计划，历史已关闭不重开。
- 顾问在原计划详情选择审核报告、确认检查完成：有后续时验证已审核发布草稿、源版本和实际后续任务（含必要服务承接）；无后续时明确理由。原任务CAS结案、重放不改完成时间。
- 76项回归通过；实际隔离HTTP双路径/权限/提前关闭保护、顾问真实页面结案通过。使用合成报告及预置AI草稿，不是真实AI验收。1900模块构建通过，最后详情人员展示微调待重建。
- 证据gu4B4J/followup-outcome-http.json；客户6ab0ec510113d874a2b76faa，页面原计划6ab0ec520113d874a2b76fd1，后续6ab0ec52c808455c8a372bfe。隔离后端73776；末次服务承接凭据检查仅单测验证，需重载HTTP复验。
- 核销HTTP脚本已区分新原计划保持打开与历史已完成保护，修订尚未新案例复跑；旧核销自动关闭不能算新标准通过。

### 剩余边界

- 已实现明确服务来源的报告/后续候选预选，尚未合并报告审核，仍有重复审核操作。现有reportFollowUpSource明确排除有关联服务的报告自动派生草稿；不能解除该规则造成重复任务，合并需复用服务顾问结果节点。
- 手工无结构类别计划未强制门槛；结构化原管理计划的服务等待入口与客户求助已通过实际HTTP/页面，未批量迁移历史数据。
- 原任务有CAS，但报告/后续任务跨文档并发修改未完整防护；测试只覆盖读取前已撤审/过期/缺任务。不宣称全部并发安全。
- 自动提醒节奏、年度首次评估生成仍待，营养/运动/中医排除。自动任务暂停，不部署/推送。

## 第一部分历史记录

- 普通随访执行默认进行中；两个员工入口均使用独立progress接口追加记录，设置下次跟进时间，保留原计划要求和历次记录。不新建随访任务。
- 当前执行人可记录，创建人不能越过实际负责人；状态、updatedAt CAS、请求标识防重复/防旧窗口覆盖；已关闭、待审核、服务岗位任务拒绝。旧executedContent在第一次追加时保留为历史。
- 实际隔离HTTP：两次记录、403权限、409旧窗口、重复提交无重复、下一次跟进日期及原任务进行中通过。真实页面再保存第三次，重新打开显示三次过程，默认进行中。
- 29项单测回归通过；员工端1898模块构建通过。测试过程发现并修复Mongoose子文档type字段歧义。尚未生产部署或远程推送。

## 第一部分当时待办（以顶部最新状态为准）

- 目前仍保留原人工“事项已全部完成”入口；未强制报告/顾问处置门槛。自动服务回写/核销关闭逻辑尚未调整，禁止以本次通过当作新闭环标准已实现。
- 下一步接明确来源的报告/顾问审核结论与实际新计划发布凭据，再阻断所有提前关闭入口（通用更新、用户完成、服务投影、体检承接、核销回写）；无需后续必须顾问确认。不要按标题/日期猜测报告归属。
- 正在服务等待中的计划过程记录入口仍需打通；本接口允许无岗位workflowKey的原计划，但当前页面部分入口仍只读。AI生成建议复用现有报告草稿/审核发布链，失败和中断保持原计划。
- 本次尚未证明所有工作台按nextFollowUpDate抑制过早提醒；仅持久化已有nextFollowUpDate/remindAt字段，不能称提醒调度已全验。

接续：隔离后端exec97355（凭证/外部出口清除、后台禁用），清单gu4B4J/session.json；测试任务6ab0de7a11daa48af6205ba3，客户6ab0d74ea236b0b3e0243fa0。首个失败模型测试fixture保留。浏览器1/tab3健管该任务弹窗，三条过程已保存。自动任务保持暂停；营养运动中医仍排除。
