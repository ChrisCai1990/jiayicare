# App 和小程序同步台账

基线：`deploy-health-monitoring-7630c892` 的当前代码。小程序为产品基线；本台账只记录从代码可验证的差异，业务策略尚未确认的项目标为“待产品验收”。

## P0 上架前处理

| 编号 | 差异 | 代码证据 | 建议处理 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- |
| APP-001 | 未登录首次入口不同 | 小程序页面配置将首页放在首位且注释说明可先浏览；App 导航在无 token 时只注册 `Login`、`Onboarding`、`Legal` | App 支持未登录浏览首页与服务介绍；在测评、建档、预约、订单等动作触发登录 | 冷启动未登录用户可到达首页；受保护动作会引导登录且回跳原路径 | 已实现，待真机验证 |
| APP-002 | 底部导航结构不同 | 小程序：首页、健康档案、健康管家、我的；App：首页、健康档案、健康规划、消息、我的 | 以小程序 4 Tab 为准；将 App 的消息入口移入首页消息入口/个人中心/Stack 路由，保留未读提示 | 两端底部入口、文案和主页面一致；消息仍可到达且未读可见 | 已实现，待真机验证 |
| APP-003 | 健康管家命名和承接不同 | 小程序 Tab 文案为“健康管家”；App Tab 文案为“健康规划”，两端均接 Chat 页面 | App 改为“健康管家”，同步首页和商店截图口径 | 导航文案、页面标题、推广物料一致 | 已实现，待真机验证 |
| APP-004 | 小程序有位置选择页，App 无对应路由/页面 | 小程序注册 `pages/profile/location/index`；App 路由和 screen 目录未见对应页面 | 判断位置是否影响本地服务、预约或价格。若影响，补齐 App；否则小程序也应移除入口 | 用户从两端获得一致的服务地域与预约结果 | 待产品验收 |
| APP-005 | 小程序有邀请页，App 无对应路由/页面 | 小程序注册 `pages/profile/invite/index`；App 未见 Invite 页面或路由 | 若邀请是现行增长功能，App 需补齐并统一分享落地页与归因；否则从小程序入口下线 | 两端邀请能力与奖励规则一致，或两端均不展示 | 待产品验收 |

## P1 首个上架后版本处理

| 编号 | 项目 | 建议 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| APP-101 | 消息通知 | 对齐未读、消息入口、通知设置和端侧推送策略 | 消息可读、未读一致、通知权限按需申请 | 待盘点 |
| APP-102 | 服务商城和服务计划 | 对比服务卡、价格、购买路径、订单状态和续费入口 | 同一服务在两端的名称、价格、规则和可购买性一致 | 待盘点 |
| APP-103 | 健康档案与报告 | 对比档案卡、报告上传、AI 健康、体检报告、筛查和公开报告路径 | 同一账号打开两端数据一致，异常状态可解释 | 待盘点 |
| APP-104 | 个人中心 | 对比家庭成员、权益、账号安全、反馈、通知、协议和注销 | 核心设置和隐私入口一致 | 待盘点 |

## 已具备的页面映射

小程序和 App 已同时具备以下能力入口：登录、引导、首页、健康档案、随访、消息、健康管家/聊天、健康记录录入、健康报告、报告上传、用药、营养、问卷、提醒、服务商城、服务计划、编辑资料、账号安全、反馈、通知、订单、协议、权益、家庭成员、AI 健康、体检报告、专项筛查、公开报告和档案摘要。

这说明 App 收口的主要工作是入口策略、页面细节与业务流程回归，而不是从零补建核心模块。

## 上架资料待收集

- 主体名称、统一社会信用代码、联系地址、客服电话与对外邮箱。
- 软件著作权证书、应用名称、图标、启动图、截图、简介、关键词和年龄分级口径。
- 隐私政策 URL、用户协议 URL、第三方 SDK 清单、权限申请用途、账号注销处理时限。
- Apple Developer 与各 Android 应用商店账号状态；签名、包名、版本号与测试包归档位置。
- 服务范围、价格、预约方式、医疗广告审查材料（如适用）和客服应答 FAQ。

## 下一步

1. 先完成 APP-001 至 APP-003 的代码和真机验证。
2. 逐页对比服务商城、档案、订单和个人中心，补充 P1 台账。
3. 同步收集上架资料；缺失项单独形成“需要业务方确认”卡片。

## 本轮实现与验证记录

- 已修改 App 导航、首页、服务商城和个人中心：访客首页/商城、购买和个人数据登录拦截、四栏底部导航、健康管家命名及服务消息入口均已落地。
- 已用 Babel JSX 解析器检查上述 4 个修改文件，语法通过；已运行 `git diff --check`，无空白错误。
- 当前工作副本缺少 App 的 Expo 依赖目录，无法在本机运行 Expo 构建或真机打包。恢复依赖后，应执行 Expo web export 和 Android/iOS 真机回归；该限制不影响后续代码盘点和文档工作。
