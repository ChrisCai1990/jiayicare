# 补充依据／更新方案

## 发布结果

2026-09-23 已部署 `6d4785df84fc3efed2d350cefe37e03b0748d422`，四端生产构建成功，公网健康与 Staff 页面均200，新资源 `/assets/index-Nny-zq_d.js`。生产只读核验年度查询包含已确认 checkup 研判（1条）。金娟白名单保持，恢复及体检自动扫描关闭，跳过数据迁移；未代用户生成AI或保存客户业务记录。已推送方案的正式修订发布仍未开放。

## 操作

年度方案页点击“加载／刷新可用依据”，勾选已确认研判（不限类型）、已审核报告，或填写并确认顾问补充意见。点击“生成调整建议”，查看调整前后及来源处理说明，逐项勾选后应用。

- 未推送：只把选中建议应用到页面草稿，仍须顾问保存、审核、推送；其他模块不删，人工医院/专家/服务选择保留。
- 已推送/已确认：只保存独立内部修订草稿，可刷新依据后查看留档。不能直接发布修订版，不能改正式方案或已有任务。原冻结及任务规则不解除。
- 取消旧事项、修订正式发布并联动任务不在本次上线范围，不能把保存修订建议称为已执行。年度重点按行追加，不自动取消原重点。

## 来源与保护

默认年度查询补上checkup类型（此前已确认体检研判被漏掉）；其他类型通过顾问显式选择，不自动把全部讨论纳入。服务端校验同客户、研判已确认且未归档、报告已审，禁止只信任客户端正文。补充意见必须顾问确认。

新依据及原方案参与生成输入指纹；输出为补充候选，不直接保存。差异确认前检查原草稿是否变化，服务端修订保存检查updatedAt；重复修订用指纹避免重复留档。已推送方案禁止旧PUT入口静默覆盖。内部修订默认select:false，客户接口不展示。

## 验证

全量1244项：1242通过/0失败/2独立Mongo集成跳过。随后新增实际修订控制器权限/归属/过期校验及仅写内部建议用例，专项6通过。Staff1912模块构建通过。

本次未代客户真实AI生成或保存业务数据；模拟/函数/API控制器测试不是生产顾问验收。仅金娟灰度保持，跳过数据迁移部署；不扩大营养、运动、中医验收范围。
