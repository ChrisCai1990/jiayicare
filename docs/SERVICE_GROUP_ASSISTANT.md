# 家庭服务助手

首次绑定群名优化：识别未绑定群后，通过当前员工已绑定的企微身份读取官方客户群名称，核验群主/内部成员身份，只返回名称，不向客户端暴露成员列表。自动预填原群名，已有绑定名称及同群未保存编辑保持不变；读取失败仍可手填且不修改企微群名。取名结束后再校验当前群，防止异步切群填错。

## 2026-09-09 第二批协作功能

- 侧边栏折叠「服务概览 · 交接与回复」显示最近200条助手事项中的待跟进及负责人/日期；个人临期视图仅显示分配给本人、24小时内到期或逾期的已确认事项。
- 一键交接、客户回复为规则模板预览，不调用AI、不自动保存或发送。交接不混入未确认草稿作为事实；客户回复必须指定具体成员，仅引用该成员已完成/已确认记录；原FollowUp状态回读优先。人工核对披露范围后才能走原保存/分享流程。
- 「读取原系统家庭关系」只列出 User.familyLinks 正式关联且当前员工有权访问的客户；逐个确认导入，关系注明相对于谁，不改变原系统家庭关系，不从旧family姓名/手机号猜身份。
- 应用文字消息回调：`/api/integrations/wecom-app`，**不是会话内容存档回调**。AES-CBC/签名/企业和应用校验，通过10分钟一次性口令绑定已登录医护员工与企微UserId，独立唯一索引避免同一企微账号绑定多人。不自动重绑定。收件箱按员工/机构隔离、AES-GCM加密、7天TTL、消息ID去重；最多查看最近30条。支持文字导入当前家庭指令/总结，仍需选择服务对象并确认。文件消息暂不下载，返回使用侧边栏报告归档的提示。
- 主动员工提醒：已绑定员工自行勾选，每日工作时段最多一条，不含客户资料；发送前复核员工状态、机构、团队、全部家庭成员权限和模块权限，原FollowUp状态优先。发送前持久占位，失败/超时不自动重发；通知不改变待办状态。每小时扫描，首轮在启动后1小时内；每次最多200员工、每员工100群/每群200事项，非全量服务台。
- 新配置：WECOM_APP_CALLBACK_ENABLED / WECOM_APP_CALLBACK_TOKEN / WECOM_APP_CALLBACK_AES_KEY / WECOM_APP_INBOX_KEY / WECOM_EMPLOYEE_REMINDERS_ENABLED。密钥独立于会话存档公私钥；保存在服务器安全运行配置，不能写入Git或聊天。群消息存档开关保持关闭。生产启用需填写自建应用的「接收消息→设置API接收」并实机测试；模拟测试通过不能当作企微已连通。
- 验证：`node --test backend/test/serviceGroupWorkbench.test.js backend/test/wecomAppCrypto.test.js backend/test/wecomEmployeeReminders.test.js backend/test/serviceGroupRoutes.test.js`，以及 `node scripts/test-service-assistant.cjs`。虚构数据涵盖未授权家庭关系排除、交接预览不保存、回复必须选成员、加密回调拒绝错签名/错企业/错应用、未知员工不入库、重试幂等、个人提醒隔离及去重。

顶部布局优化（2026-09-09）：去除品牌副标题及宣传语，接入状态和识别重试收进默认折叠区；群内已有家庭不再重复展示绑定名称，家庭概览压缩、统计改为行内数字，保持错误与操作结果可见。390px模拟页面视觉核验及8个交互场景通过，增加功能导航顶部不超过340px的布局断言。无业务数据或权限变更。

当前群模式（2026-09-09）：企业微信UA且embedded=1时自动识别当前群并锁定其家庭，隐藏群切换和通用新建入口；群标识只读。已绑定直接显示家庭，未绑定显示首次绑定；重新识别未绑定群会清空之前家庭。业务操作前再次校验当前群标识，不一致则停止操作。独立医护浏览器保留多群切换。模拟SDK页面回归覆盖自动选择、隐藏切换、未绑定群清除旧家庭；构建及8个页面场景通过。真实企微客户端仍需用户回读验证。

客户端诊断v3：用户已定位到 getCurExternalChat 无状态文字。兼容仅含有效 chatId 的返回，但任一明确失败状态或非零错误码仍拒绝；发送仍要求显式成功。此项为返回格式兼容修复，尚需真实客户端回归，不把缺失状态直接等同于权限不足或识别成功。

客户端诊断（2026-09-09）：真实企微仍返回操作失败，尚不能确认具体权限或客户端原因。前端诊断v2区分 config、agentConfig、getContext、getCurExternalChat，保留SDK错误码并隐藏长标识及链接，不收集完整响应或群内容。三项诊断回归与医护端构建通过；需用户再次识别提供具体错误，不应要求反复重登或直接扩大权限。

应用连接进度（2026-09-09）：用户确认可信域名与企业可信IP保存后，生产应用认证、企业及应用JS票据、侧边栏双签名均验证通过，四项WECOM身份/来源参数已保存到生产安全运行配置，后端重启与健康检查通过。Secret未写入仓库或日志。群采集与AI开关仍关闭；接下来由用户在真实群内刷新助手并验证“识别当前企微群”，不能将服务端签名通过视为客户端群识别已经完成。

域名校验进度（2026-09-09）：用户提供的 `WW_verify_2o37kcBTBvlM3kCB.txt` 已原样上传生产 `staff/public/` 与 `staff/dist/`，公网同名根路径 HTTP 200、16字节逐字节一致。文件为服务器未跟踪的域名校验静态资源，保留 public 源文件以便 Vite 后续构建复制；更换服务器时须迁移。企微后台是否已校验并保存可信域名仍待用户确认，本操作未配置应用Secret或开启群采集。

## 2026-09-09 发布准备

发布结果：`9a22de02` 已完成生产全量部署，四端构建与API健康检查通过；正式浏览器已显示嵌入式家庭服务助手空态。入口为 `https://staff.jiaycare.com/service-assistant?embedded=1`。未执行真实家庭绑定、报告上传、AI调用或对外发送。企微应用身份与可信域名配置、群识别实机验证仍未完成。

用户已确认部署。发布基线保留 GitHub `4701f0fc` 及生产额外的 `74321859`、`d76457b3` 两项会员建档修复；家庭助手提交为 `4738c22b`。部署前核验生产 tracked clean，保留全部未跟踪运维文件；五项既有迁移标记均存在，不重跑业务迁移。企微身份配置尚未设置，群采集和AI开关均未启用。

合并后验证：助手9项、医护端21项、页面7个场景通过，医护端构建通过。全量后端275项中271项通过、4项失败：`aiCaseReviewRouteGate` 路由数量断言与现有代码不一致；`inpatientPlanPermission` 三项缺少现有路由依赖的测试替身。上述测试及对应路由相对生产基线没有改动。不能将此记录视为已完成真实企微或生产健康数据写入验收。

入口：医护端「家庭服务助手」`/service-assistant`。企微侧边栏配置入口使用 `/service-assistant?embedded=1`，隐藏医护端导航，继续使用现有员工登录。尚未接入企微免登。

## 已实现的业务流程

| 场景 | 当前实现 |
| --- | --- |
| 个人 / 家庭服务群 | 创建群档案、绑定一个或多个已有客户、标注关系、配置负责人和服务团队、绑定企微 chatId |
| 现有数据 | 按所选家庭成员读取 JiayiCare 随访、服务记录、报告；遵守当前机构、个人分配和模块权限 |
| 待办 | 保存草稿、编辑、确认、开始处理、填写完成结果；个人事项确认后写入 FollowUp，回读原系统状态；家庭共同事项保存在服务群事项中 |
| 沟通记录 | 草稿确认后，个人记录写入 ServiceRecord 的 group_service 类型；家庭记录留在群档案，不自动归给某个成员 |
| 服务总结 / 交接 | 手动记录；配置AI并确认本群授权后，可整理所选沟通文本，生成总结和附原文证据的候选待办。候选待办需明确成员、人员、日期后确认 |
| 报告 | 选择文件与具体成员、分类、日期后归档；仅上传、不自动解析或审核。PDF/JPEG/PNG/WEBP，单个文件20MB以内 |
| 群通知 | 拟稿、确认、复制、人工标记已通知；企微环境下可校验当前 chatId 后调用分享。分享成功与客户已读、待办完成分开 |
| 快捷指令 | 嘉医汇待办 / 记录 / 总结 / 归档 + 冒号；只预填草稿，不猜“妈妈”的档案，不把普通聊天当作归档授权 |
| 群消息接入 | 签名桥接接收器、按群授权、消息ID幂等、AES-GCM加密、30天到期清理；可接收文本及9MB以内base64报告。收件箱可转为待办、总结或选定成员后归档 |

### 仍需配置、开发适配或实机验证的部分

- **已部署生产页面及后端**；没有写入真实客户数据、调用真实AI、发送群消息或修改企业微信后台。
- 管理员需创建自建应用、配置可信域名与客户联系接口授权。签名和客户端接口已实现，但尚未在真实企微群验证。
- **桥接接收器不是企业微信官方回调地址**。还需要在管理员控制的环境接入企业微信会话内容存档官方SDK/授权服务商采集器，将解密、确认可处理的消息按下面协议转入本系统。本仓库没有安装或运行官方Finance SDK采集器，不能声称已监听真实群。
- **群里一句话自动执行归档尚未启用**。当前接入消息和口令进入可确认收件箱；工作人员在助手中明确文件和人员后归档。没有机器人成员，也没有自动群回复。
- AI仅按按钮生成，不做后台无人值守决策。支持从多条收件箱消息组合输入，或手动粘贴。
- 原件SHA-256去重覆盖带 `sourceSha256` 的报告（本入口新上传数据）；旧报告未做摘要回填，不能把文件名去重当成原件去重。
- 本轮单文件归档；多张照片组成同一报告仍使用既有报告上传页面。PDF真实总页数由既有详情/预览接口读取原件取得，不能把历史 `pages` 默认值作为真值。
- 个人待办复用原FollowUp，家庭共同事项独立存在此工作台；尚未扩展全局首页的家庭事项提醒，也未实现定时自动群推送。
- 新模块采用更严格的本人分配访问控制：服务团队授权不替代个人档案分配；暂不继承下属/导师的扩展客户范围。
- 当前上限：最近100个群、每群200条事项、收件箱最近100条；不应作为全量历史导出。

## 接入配置

仅在安全运行配置中设置下列变量，不将真实值写入Git或聊天：

| 变量 | 用途 |
| --- | --- |
| WECOM_CORP_ID / WECOM_AGENT_ID / WECOM_APP_SECRET | 自建应用身份 |
| WECOM_SIDEBAR_ORIGIN | HTTPS可信来源，如 `https://staff.jiaycare.com`，不带路径或末尾斜杠 |
| SERVICE_GROUP_AI_ENABLED | `true` 才开放AI总结，另需既有 QWEN_API_KEY 或 DEEPSEEK_API_KEY |
| SERVICE_GROUP_ARCHIVE_ENABLED | `true` 才接受签名消息并启动到期清理 |
| SERVICE_GROUP_BRIDGE_SECRET | 仅机构控制的采集器与后端共享的高熵签名密钥 |
| SERVICE_GROUP_BRIDGE_TENANT_ID | 采集器所属机构ID；未设置仅匹配历史 tenantId:null 群，不跨机构 |
| SERVICE_GROUP_MESSAGE_KEY | 32字节AES密钥的64位十六进制表示；轮换时需处理旧消息解密 |

文件继续使用现有私有OSS配置。企微可见范围、服务群团队、每个客户的员工分配和自定义角色权限均须一致。模块入口要求 `patients.view`、`service_records.view`，其他操作按对应模块 create/edit/view 验证。

### 消息桥接协议

`POST /api/integrations/service-groups/messages`

请求头：`x-jy-timestamp` 为当前毫秒时间戳；`x-jy-signature` 为 `HMAC-SHA256(secret, timestamp + '.' + 原始JSON请求体)` 十六进制。接受正负5分钟窗口。同群同 messageId 幂等。

正文示例为虚构内容：

```json
{
  "chatId": "官方当前群标识",
  "messageId": "官方稳定消息标识",
  "sender": "发送者标识或已授权显示名",
  "sentAt": "ISO时间",
  "consent": true,
  "text": "嘉医汇待办：核对预约时间",
  "file": { "name": "report.pdf", "base64": "仅文件消息提供此字段" }
}
```

采集器必须核验官方会话存档授权范围及成员同意状态，不能仅因为有人@机器人或加入服务群就设置 consent:true。只有管理员明确启用且群负责人已确认 archiveConsent 的绑定群可接收。消息只接受最近30天，原文加密；文件拷贝保存到私有 `service-group-staging/`，到期先删除该暂存对象再删除消息元数据；正式报告位于 `reports/`，不受暂存清理影响。

无需把存档密钥或企业微信Secret发给客户端。读取群消息、生成AI草稿、保存个人档案分别受控。

## 验证与运行

2026-09-09 侧边栏再次进入：同群可见性校验保留已加载家庭、设置和未保存绑定表单；只有确认切群才清空旧家庭。WebView 完整重建时仍先验证当前群，再读取服务端绑定，不持久缓存上次家庭冒充当前群；此阶段显示“正在读取当前群的绑定关系”，不显示未绑定空态。自动识别只读取绑定，不创建或重新保存绑定。虚构浏览器回归覆盖延迟初始化、同群编辑保留和切到未绑定群的隔离。

2026-09-09 加载优化：嵌入侧边栏不再两次读取全部服务群再获取详情，而通过 `GET /by-chat/:chatId` 一次返回当前群的授权数据包，复用详情接口的群、患者和随访权限校验。读取后再次确认群身份，避免请求期间切群串户；同群回访更新数据但不重置表单。无权限读取会清空旧数据，不以无权限冒充未绑定。合成浏览器测试断言初次侧边栏只有一次当前群数据请求、没有全量列表或重复详情请求；不宣称真实企微客户端零加载时间。

```powershell
node --test backend/test/serviceGroupRules.test.js backend/test/serviceGroupRoutes.test.js
npm --prefix staff run build
```

页面回归（使用虚构内存数据，不连接MongoDB、OSS或AI；所有浏览器API请求拦截至本地fixture）：

```powershell
# 终端一
npm --prefix staff run dev -- --host localhost
# 终端二
node scripts/test-service-assistant.cjs
```

默认查找 Windows Chrome，可用 `TEST_CHROME_PATH` 指定浏览器；`TEST_STAFF_URL` 可指定本地Vite地址；`TEST_ARTIFACT_DIR` 可指定截图目录。截图默认在 `artifacts/service-assistant/`，全部为虚构人物。

手动连接真实本地开发库时，应明确设置 `VITE_API_URL=http://localhost:3000/api`，不要误用医护端既有的生产API默认值。此页面首次上线前应在测试环境完成MongoDB唯一索引、OSS真实上传、企微群识别与发送验证。

2026-09-09：新增流程测试、真实路由+内存持久层集成测试、桌面与390px侧边栏交互回归通过。全量后端回归发现既有 `inpatientPlanPermission.test.js` 缺少 `upsertMedicalAssistModuleTasks` VM替身导致失败；该测试及被测 `staff.js` 本轮未修改。此限制不可用服务助手定向测试通过来掩盖。
