# JiayiCare 项目记忆

## 2026-08-19：OCR 审核版本与用户只读边界（本地未部署）

- 医护端 OCR 审核已拆分为识别快照 `ReportExtraction`、正式审核版本 `ReportRevision` 和审核后专项筛查投影；正式版本记录审核人、审核时间、来源识别版本及内容哈希，重复提交不会重复创建相同版本。
- `MedicalReport.reportItems` 继续作为医护端可编辑工作副本；用户报告接口通过 `reportUserView` 只返回正式审核版本。未审核 OCR 项目、AI 摘要、纠错日志、审核人及内部版本引用不向用户端下发。
- 已审核报告重新 OCR 时，用户继续看到上一版 `ReportRevision`；历史数据尚未回填版本时回退到 `staffAuditSnapshot`，避免新草稿覆盖已审核内容。App、小程序代码本轮不修改，原接口字段保持兼容。
- 专项筛查自动归类只在正式审核提交后同步，记录 `reportRevisionId` 和 `sourceItemIds`；只清理 OCR 审核机制生成的过期投影，不影响人工或历史记录。
- 正式版本中未归类的项目写入独立 `ReportScreeningCandidate` 医护待办，不阻塞报告发布，也不直接进入 `UserScreeningItem`；新审核版本产生后，旧版本未处理候选标记为 `superseded`。医护端版本留痕区显示当前待归类数量。
- 医护可在版本留痕区对候选执行“确认归类”或“不进入”；确认前服务端会校验当前审核版本和动态分类目录，使用 `resolving` 状态防止并发重复处理，成功后写入带审核版本/来源项目引用的筛查投影，并记录处理人、处理时间。分类选择同时回写医护工作副本，供后续新审核版本沿用，但不修改既有不可变审核版本。
- 待归类任务已具备可发现性：会员报告列表返回每份报告的 `pendingScreeningCandidateCount`，提供“待归类”筛选、状态徽标和处理入口；医护统一待办按报告聚合候选数量，不逐项目刷屏。
- 医护端报告上传改为临时凭证闭环：预识别所需原件在取消、换文件或建档失败时回收，清理接口只删除尚未被报告引用的 OSS 对象；创建请求携带医护人员范围内的幂等键，网络重试和并发重试不会重复建档。旧调用方不携带凭证或幂等键时仍保持兼容，稀疏唯一索引不会给旧记录写入空键。
- 医护端多文件上传在提交前显示顺序编号、文件大小，并支持上移、下移和移除；“同一份报告多页”与“多份不同报告”改为显式二选一，合并模式提示页序会成为 OCR 阅读顺序，降低误合并和错页风险。
- 医护端上传凭证逻辑抽取为独立校验模块并限制单次最多 50 个文件；上传后的医院/日期快速识别不再接受前端任意 URL，只能使用绑定当前医护人员、OSS key、文件类型和大小的短时凭证读取原件。其他医护、缺失或过期凭证均拒绝，相关凭证与用户只读边界测试共 17 项通过。
- 新增 `TemporaryReportUpload` 服务端登记：原件上传后为 `temporary`，建档请求以独立尝试标识抢占为 `attaching`，成功后关联 `MedicalReport` 并转为 `attached`；取消清理只抢占仍可回收状态，删除前检查报告引用，删除结果和错误均保留审计记录。超期回收脚本默认仅预演，真实删除必须提供 `--apply`、显式 `--limit` 和确认环境变量，并在抢占后再次检查报告引用；本地只读预演扫描 0 条、未写库或删除 OSS。登记记录不使用 Mongo TTL，以免先删证据再漏掉 OSS 对象。
- 审核证据拆为内容版本与动作台账两层：`ReportRevision.review` 使用显式字段固定审核人 ID/姓名/角色、审核时间和动作，`source` 固定来源 OCR 版本；新增追加式 `ReportReviewEvent`，即使内容哈希相同而沿用既有版本，也记录本次正式提交。医护端审核请求携带报告范围内的幂等标识，重试直接返回既有审核结果；审核页显示发布版本的审核人、时间、OCR 来源及逐次正式审核操作。历史回填预演额外报告审核事件数量及身份/原始时间/识别来源缺口，缺失证据只标记、不推断补造。当前相关模型与边界测试 22 项通过，医护端构建通过。
- 审核提交的重试不再只看“版本/事件已存在”就返回成功：会重新对账当前版本的待归类候选、已归类 `UserScreeningItem` 投影和身体成分同步，覆盖请求在下游同步前中断的半完成状态。候选或筛查投影同步失败不再被日志吞掉后仍返回成功，而是让请求失败并可用同一审核请求标识安全重试。完整后端回归为 112 项中 107 通过、5 项既有失败（真实人员修复脚本、邵逸夫/浙一旧模板断言），本轮新增链路未增加失败项。
- 医护端删除未审核报告时新增版本保护：只要已存在 `ReportRevision`，即使历史 `audit_status` 不一致也拒绝直接删除。允许删除的草稿会级联清理识别快照、候选、审核事件和报告来源筛查项；OSS 原件删除前写入/更新 `TemporaryReportUpload` 清理登记，删除失败不再让已经消失的报告返回不可重试的失败，而是返回“记录已删除、原件后台重试”，登记保持 `cleanup_failed` 并由超期回收脚本继续处理。相关定向测试增至 23 项通过。
- 报告审核高风险动作改用严格权限校验：自定义角色必须显式拥有 `reports.audit` 才能启动/重跑 OCR、单页补提、编辑草稿、正式提交审核或处理待归类候选；报告删除必须显式拥有 `reports.delete`。已绑定角色但角色被删除时返回 403，权限存储异常时返回 503，不再故障放行；未启用自定义角色的历史员工继续兼容。医护端同步隐藏无权限操作并显示只读提示，默认角色权限补齐 `reports.delete:false`。权限与核心边界定向测试共 28 项通过，医护端构建通过。
- 医护端独立“报告管理”页已移除旧 Base64/单文件/约 7MB 上传弹窗：顶部“上传报告”先选择会员，再跳转会员报告页并自动打开唯一的安全上传弹窗，因此多页顺序、100MB 单文件限制、临时凭证、OSS 登记、取消回收和幂等建档不再因入口不同而分叉。报告管理页的修改/审核按钮同步遵守权限；已有 OCR 草稿的报告不能从列表直接“通过”绕过事实核对，服务端同样返回 409，页面改为“前往核对 OCR”。核心定向测试增至 31 项，医护端构建通过。
- 报告权限新增 `reports.create`（管理端显示为“新增”，业务含义为上传报告原件/建档）。新自定义角色可只上传不审核；已有自定义角色若已具备 `reports.audit` 继续拥有上传权限，避免升级中断；仅查看角色不能上传原件。后端保护原件上传、报告建档和快速元数据识别接口，客户端两个上传入口同步隐藏；临时文件清理仍只依赖本人签名凭证，避免权限调整后无法回收已上传对象。权限及核心定向测试 34 项通过，医护端与管理端构建通过。
- 医护端报告建档现在必须携带至少一个由当前医护临时上传凭证验证的 OSS 原件；旧 Base64/空文件建档路径停止接受新请求，MongoDB 不再重复保存一份 Base64 健康原件。PDF、常见图片及 HEIC/HEIF/BMP 的允许口径在上传过滤和建档校验间统一。原 `deleteFile` 会吞掉 OSS 异常，导致 `cleanup_failed` 永远无法触发；新增 `deleteFileStrict` 专供报告取消、删除和超期回收，失败会真实进入可重试登记，旧业务继续使用宽松函数保持兼容。核心定向测试增至 37 项，医护端构建通过。
- 用户报告兼容视图进一步收紧：正式版本、历史审核快照和旧已审核报告中的项目都会剔除 OCR 置信度、证据文本、质量标记、分类匹配分数、内部来源标识等医护工作字段；顶层同时隐藏 OSS 对象 key、上传人、幂等键、租户和内部关联 ID。用户端继续只接收原有展示字段和签名后的原件 URL，App/小程序无需改动。用户边界、审核版本和筛查投影相关 15 项测试通过。
- 医护端报告建档新增会员与方案关联校验：目标会员必须在当前租户查询上下文中真实存在；关联方案与方案项目必须成对提供，且方案必须属于该会员、项目必须真实存在，避免孤儿报告和跨会员错误回填。报告列表、详情、识别快照、审核版本、审核事件和待归类候选的读取权限统一改为严格失败关闭；未配置自定义角色的历史员工仍兼容，角色失效或权限存储异常时不再放行敏感报告读取。相关核心定向测试增至 40 项通过。
- 正式审核状态机进一步收紧：`aiStatus=reviewed` 必须同时携带 `reviewAction=submit` 和报告范围内的审核请求标识，草稿状态不能伪装成正式发布；人工审核只接受明确的通过/驳回动作并同样要求幂等标识。OCR 驳回和人工驳回现在也写入追加式 `ReportReviewEvent`，允许在尚无正式版本时以空 `reportRevisionId` 留下操作者、时间、来源和内容哈希。医护端在请求失败后保留同一审核请求标识，降低网络断开重试产生重复审核动作的风险；版本留痕区可识别驳回事件。核心定向测试增至 44 项，医护端构建通过。
- 人工审核附带的异常复查记录与用户任务改为按 `<reportId>:<reviewRequestId>` 稀疏唯一来源键幂等 upsert，并在每次重试补齐 `AbnormalReview.taskId` / `Task.abnormalReviewId` 双向引用；并发重复插入的失败方读取唯一索引胜出的记录继续对账。这样在复查记录已建、任务未建或关联回写中断时不会重复生成待办。相关核心定向测试增至 48 项；完整后端回归 141 项中 136 通过，5 项仍为既有真实人员修复脚本与邵逸夫/浙一旧模板断言，医护端构建通过。
- 医护端版本留痕新增只读“审核派生一致性”诊断：以当前 `ReportRevision` 为基准，对账非驳回审核事件、未归类候选、OCR 专项筛查投影及其版本引用，明确报告缺失候选、缺失投影、残留记录或旧版本引用；异常时提示保留报告并联系管理员，不自动写库或诱导重复人工录入。诊断区分 OCR 正式提交与纯人工审核，后者不按 OCR 自动投影规则产生假告警。相关核心定向测试增至 53 项，医护端构建通过。
- 一致性异常新增受控“重新对账派生数据”：仅 `reports.audit` 可调用，必须携带幂等请求标识；按当前不可变审核版本补齐候选和 OCR 筛查投影、收敛当前版本的多余待处理候选，并写入 `action=reconcile/source=integrity_repair/result=reconciled` 审核事件。修复事件不能替代原始正式审核事件，原审核身份缺口仍保持告警。同步逻辑同时修复了审核重试会误删“候选人工归类投影”的问题，现在会保留当前版本所有已解决候选的投影键。医护端异常提示区提供二次确认按钮，失败重试沿用同一请求标识。相关核心定向测试 56 项全部通过，医护端构建通过。
- 新增 `npm --workspace backend run verify:report-e2e` 隔离端到端验收：只连接 `127.0.0.1` 随机 `jiayicare_report_e2e_*` 数据库，不调用 OSS/OCR，通过真实 Express 路由验证受签名原件登记建档、正式审核版本、审核事件、未归类候选、筛查投影、故障检测与重新对账、用户只读字段过滤。首次运行由诊断发现一致性接口漏选 `action` 字段并已修复；失败测试留下的 3 个专用空库已按严格前缀删除，脚本关闭自动索引避免 drop 后重建。最终 E2E 全链路通过，复查 `remainingE2EDatabases=[]`。
- 医护原件上传不再只信浏览器声明的 MIME：写入 OSS 前按文件头识别 PDF、JPEG、PNG、GIF、WEBP、HEIC/HEIF、BMP，无法识别或声明与内容不一致时在外部上传前返回 400；HEIC 转换失败时也使用正确 `.heic/.heif` 扩展名，BMP 使用 `.bmp`，不再退化成 `.bin`。上传安全定向测试 23 项通过，隔离真实路由 E2E 再次通过且测试库清理为 0，医护端构建通过。
- 历史版本回填脚本默认仅预演，真实写入必须同时提供 `--apply` 和确认环境变量。2026-08-19 本地只读预演为 1 份报告需补 1 个识别快照、0 个审核版本；未执行写入。
- 新增用户只读边界测试 4 项全部通过，医护端构建通过。完整后端测试当前 96 项中 91 项通过；5 个既有失败来自浙一旧页码模板断言和依赖真实人员数据的修复脚本，与本轮修改文件无交集。
- 当前改动未提交 Git、未部署、未写入数据库；上线前仍需完成受控回填、接口联调和真实医护账号验收。

## 2026-08-18：OCR v2 本地审核减负基础

- 本地 OCR 主链路已增加：原生 PDF 文字层优先取证（缺少 `pdftotext` 时安全回退视觉 OCR）、既有机构模板标识、字段级数值范围复核、跨页重复标记、关键字段双证据、例外优先审核，以及人工修改留痕。
- 医护端报告列表已提供独立的 `OCR v2（测试）` 按钮；原 `AI解析` 强制走旧版，新按钮仅让当前报告进入 v2，均保留人工待审核，不需要为单份测试报告修改服务器环境变量。
- 已审核报告显示 `重新用 OCR v2`，必须二次确认；重跑会保留原始PDF，但清除当前健管及健康顾问审核状态并重新进入待审核，后端拒绝没有显式确认参数的已审核重跑请求。
- OCR v2 异步解析会写入报告级 `ocrProgress`：原件/文字层、逐批页面识别、覆盖复核、关键补提、质量校验与完成/失败。医护端在现有5秒轮询中展示该阶段说明，避免长报告只显示笼统“识别中”。
- 人工标准不再继续无边界堆入长提示词；按模型抄录、程序校验、模板规则、人工审核四类维护。规则迁移说明见 `docs/OCR_V2_POLICY_MANAGEMENT.md`。已于 2026-08-18 部署为默认关闭的服务器影子能力：仅 `OCR_V2_REPORT_IDS` 白名单报告进入 v2，全局开关保持关闭；本地质量规则测试和医护端构建已通过。
- 28 页原生 PDF 本地回归已完成：文字层 28/28 页可用，OCR v2 保存 115 项，自动通过 86 项、普通复核 22 项、高优先级 7 项。实测补充了科室病史/家族史文字层恢复、明确正常原文自动分流、摘要重复项移除、无范围异常降级和异常影像缺诊断标记；服务器仅只读获取模型配置，未部署或写入生产。

## 2026-08-18：OCR 审核 PDF 翻页性能

- OCR 审核弹窗原先把 `activePage` 作为 PDF iframe 的 React key；每次切页都会卸载并重建浏览器 PDF 阅读器，即便预览 API 已支持 HTTP Range，仍会重复请求报告。
- 已移除该 key，保持同一个 iframe 并通过 `#page=` 在同一阅读会话内定位；后端继续使用短时预览令牌与 `private, no-store`，不因性能修复降低原件访问边界。

## 2026-08-18：用户端报告上传分段入 OSS

- App 不再把一份报告全部页面的 Base64 一次提交到创建报告接口；改为每页最多 15MB、顺序上传至 OSS，全部成功后才创建报告记录，避免多页报告超过 JSON 请求上限。
- `/api/reports/upload-base64` 现按文件头校验 PDF/JPG/PNG/WEBP/HEIC，上传成功后仅向当前用户签发 30 分钟上传凭证。创建报告时优先以凭证内的 OSS key 和 URL 为准；中途上传或建档失败时 App 调用受凭证约束的清理接口回收该次对象。
- 本机 Expo 依赖目录被占用，无法完成 Web 构建；上线前由部署脚本的干净依赖安装环境完成构建并核验。后端路由已通过 `node --check` 与 `git diff --check`。

## 2026-08-18：私有 PDF 预览分段读取

- 医护端报告预览继续通过短时、报告与文件索引绑定的 API 令牌访问私有 OSS；预览代理现只透传受校验的单段 `Range` 请求，并返回 OSS 的 `206`、`Content-Range`、`Content-Length`、`Accept-Ranges`。大 PDF 可由浏览器优先读取当前页所需字节，避免等待整份文件流式完成。
- 仍保持 `Content-Disposition: inline`、`private, no-store` 和 `no-referrer`；不因性能优化改为公开 Bucket 或长期直链。

## 2026-08-18：历史报告 OSS 预览签名修复

- 历史报告迁移至私有 OSS 后，会员详情页的 `GET /api/staff/patients/:id/reports` 曾直接返回原始对象 URL，AI 审核弹窗会出现 `AccessDenied`。该接口现与其他报告读取接口统一通过 `withSignedReportFiles` 签发短时预览 URL。
- 修复提交 `e11e471` 已部署；生产环境已验证签名 URL 返回 200。该变更不修改报告记录或 OSS 对象。
- 个别历史 PDF 的 OSS 响应会强制下载，无法在 OCR 审核弹窗逐页核对。`e473f0d` 增加了仅绑定报告/文件、30 分钟失效的预览令牌（时效由 `4e97610` 从 10 分钟调整），由已部署 API 从私有 OSS 流式转发并强制 `Content-Disposition: inline`；生产验证 PDF 预览响应为 200。
- 预览地址必须包含 `/api` 前缀：医护端的 `API_ORIGIN` 不含该路径，缺失时会误进入用户端 SPA 并显示登录页。`c33874d` 已修正并验证完整预览地址返回 `200 application/pdf inline`。

## 2026-08-18：健康原件 OSS 收口

- 医护端体检报告、专项筛查、服务记录附件改为内存直传 OSS，不再写入 ECS `uploads/`；关联对象路径保存到 `MedicalReport.ossKey/ossKeys` 和 `ServiceRecord.attachments[].ossKey`，删除对应业务记录时清理对象。
- 快速 OCR 与完整 OCR 同时兼容 OSS URL 及历史 `/uploads/`；PDF 仅在系统临时目录渲染，不进入业务存储。用户端新的 base64 报告不再在 OSS 失败时降级写入 MongoDB，失败会清理已上传对象并提示重试。
- 历史 `uploads/` 暂不迁移；迁移前先完成 Mongo 引用、本地文件和容量的只读对账。

### 历史报告迁移执行状态

- 已完成只读对账：`uploads/` 共 3,209 个文件、约 5GB；`MedicalReport` 关联的 2,433 个历史本地文件均存在，另有 776 个未引用文件，均不在本次迁移或清理范围内。
- 迁移工具：`backend/scripts/migrate-local-reports-to-oss.js`。仅复制报告原件至私有 OSS，写入成功后才更新 `MedicalReport.fileUrl/fileUrls/ossKey/ossKeys`；源文件不删除。实际写入需同时提供 `--apply`、`--limit` 和 `MIGRATION_CONFIRM=health-file-copy-v1`。
- 已验证首批 10 份与后续 100 份：数据库指针、OSS 对象可读性和本地源文件均一致。后续批次在服务器以每批 100 份继续执行，任一批迁移或校验失败即停止，不自动清理旧文件。

更新时间：2026-07-15

## 权威入口

- 当前架构、运行方式、线上入口和开发约定以本项目 `AGENTS.md` 为准。
- Claude Code 时期的详细功能变更史已归档到 `D:\codex\brain\archive\jiayicare_changelog.md`，仅用于追溯，不覆盖当前代码和 `AGENTS.md`。

## 长期上下文

- JiayiCare 是健康管理全链路 monorepo，覆盖用户端、医护端、管理端和后端。
- 部署完成后必须验证真实线上结果，并提供当前有效的测试入口；账号口令不得写入项目文档或记忆文件。
- 涉及生产数据删除时，应先列出具体影响记录并按当前权限要求确认；随访取消、状态过滤和重复操作要关注幂等性。

## 当前关注点

- 真实设备与主业务链路验收仍应作为发布验证重点。
- 聊天相关重构、撤回能力、历史报告缺失和 AI 授权残留属于旧记忆中反复出现的风险点；处理前先以当前代码和 issue 状态复核，不直接视为未完成任务。
- 旧记忆中的服务器、Railway、测试账号和链接可能已过期，不作为当前部署依据。

## 最近变更

- 2026-08-17：医护端视觉基线收敛为暖灰背景、深墨绿文字、森林绿单一强调色，风险/提示色仅用于语义状态；侧栏由 emoji 改为统一的 Lucide 线性图标，新增 `staff/src/components/AppIcon.jsx` 作为后续业务页图标替换入口。会员详情中已替换心理评估、ASCVD 标题图标；其余历史页面仍有零散 emoji，后续改动应优先通过 `AppIcon` 收敛，避免再新增 emoji 图标。
- 2026-08-17：医护端会员详情「体检报告」Tab 改为一张总表（报告名称、类型、机构、检查日期、状态、操作），不再按年份或类型生成卡片；顶部支持按年份及标题/机构关键词及全部、待解析、解析中、待审核、已审核筛选；“仅看信息待补”作为独立开关，可与状态筛选叠加。默认待处理优先排序，每页 20 条。缺失机构/检查日期显示“待补”。分页改为紧邻筛选栏右侧的「上一页 / 第 n 页 / 下一页」轻导航，避免需滚动到表格底部才可翻页；底部仅保留当前显示范围。操作列只保留当前主操作与省略号“更多”，点击报告名称直接查看详情，编辑/删除在“更多”内展开；待审核的 AI 审核操作使用低饱和紫色，降低整列视觉重量。表格采用 11-13px 字级、低对比度辅助信息、紧凑行距、轻分隔线和纯文本类型。会员详情风险概览在无已确认风险时压缩为单行，并明确风险标签审核状态；低频 Tab 收入“更多”，用药 Tab 缩写为“用药与营养”。修复本地医护端默认 API 指向 HTTP 服务器 IP 后被 HTTPS 重定向、证书域名不匹配而 `Failed to fetch` 的问题，默认改用 `https://jiaycare.com/api`。
- 2026-07-15：完成 Claude Code 项目记忆提炼；详细历史归档，保留当前 `AGENTS.md` 为主要权威来源。
